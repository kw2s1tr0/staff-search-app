# Repo Context

- Laravel アプリ本体は `my-app/` 配下にある
- 主要な共通文書は `AGENTS.md`、`docs/instructions/ai-working-rules.md`、`docs/shared/project-context.md`
- Laravel 関連の確認コマンドは原則として `my-app/` で実行する
- 一時的な調査メモはスキルに残さず、必要なら `docs/workbench/` を使う
